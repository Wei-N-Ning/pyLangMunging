import sys
import os

# to parse a da's feature file:
# read file into a buf
# pass the buf to the parser
# jsonify the resulting dict

# i have to write a visitor to make use of the resulting dict/tree

class Handler:

    def handle(self, node):
        pass


if __name__ == '__main__':
    this_file = ''
